winver=auto # Set the Windows version. Options: auto, 2000, xp, vista, 7. 8, 8.1, 10. Default: auto
wget_version=auto # Set the wget version that is used. Options: auto, 2000, xp, win. Default: auto
architecture=auto # Set the architecture. Options: auto, x86, x64. Default: auto
check_for_updates=true # Checks for pmfow updates when running the pmfow update command. Options: true, false. Default: true
only_check_for_updates=false # Only checks for pmfow updates when running the pmfow update command instead of updating files. Options: true, false. Default: false
only_update_one_file=false # Only updates the repository file that corresponds to your Windows version when running the pmfow update command. Options: true, false. Default: false
list_uninstallers=false # Lists all uninstallers when running the pmfow list command instead of the list of programs you can install. Options: true, false. Default: false
```